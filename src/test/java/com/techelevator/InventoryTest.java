package com.techelevator;

import org.junit.Assert;
import org.junit.Test;

public class InventoryTest
{
    @Test
    public void returns_all_data_regarding_inventory_item()
    {
        // Arrange and Act
        Inventory inventory = new Inventory();


        // Assert
        Assert.assertEquals( , inventory.getItemLocation());
        Assert.assertEquals( , inventory.getItemName());
        Assert.assertEquals( , inventory.getItemPrice());
        Assert.assertEquals( , inventory.getItemQuantity());
        Assert.assertEquals( , inventory.getItemType());




    }
}
